import React from 'react';
import { Text, View } from 'react-native';

const Award = ({ navigation }) => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>The Award screen</Text>
    </View>
  );
};

export default Award;
