import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Equation = () => {
  return (
    <View>
      <Text>The equation screen</Text>
    </View>
  );
};

export default Equation;

const styles = StyleSheet.create({});
